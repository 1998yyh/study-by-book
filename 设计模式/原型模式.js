/**
 *  js 根本 原型链等等
 * 
 */


//  deepclone
function deepClone(params) {
  if(typeof params !== 'object' && params !== null){
    return params;
  }
  let copy = {};
  if(params.instanceof())
  for(let i in params){


    
  }
}